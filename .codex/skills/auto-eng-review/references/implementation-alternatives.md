# Implementation Alternatives

Before committing to a plan, produce 2-3 distinct implementation approaches. This is not optional.

For each approach:

```
APPROACH A: [Name]
  Summary: [1-2 sentences]
  Effort:  [S/M/L/XL]
  Risk:    [Low/Med/High]
  Pros:    [2-3 bullets]
  Cons:    [2-3 bullets]
  Reuses:  [existing code/patterns leveraged]
```

Rules:
- At least 2 approaches required. 3 preferred for non-trivial plans.
- One approach must be the "minimal viable" (fewest files, smallest diff).
- One approach must be the "ideal architecture" (best long-term trajectory).
- These two approaches have equal weight. Do not default to minimal just because it is smaller.
- If only one approach exists, explain concretely why alternatives were eliminated.
- Do NOT proceed without user approval of the chosen approach.

Recommend one with a one-line reason mapped to engineering preferences.
