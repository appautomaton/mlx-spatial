# Intake: mlx-spatialkit UV Surface Fill Gate

Generated by auto-office-hours on 2026-05-27
Active change: 2026-05-27-mlx-spatialkit-uv-surface-fill-gate
Status: DRAFT
Mode: Builder
Work scale: feature
Work shape: mixed

## Objective
Add bounded native UV-surface hole fill so native-chart global texture coverage can clear the `0.50` floor without relaxing thresholds.

## Broader Intent
Turn `mlx-spatialkit` into the dependable native export backend for `mlx-spatial`: quality-first geometry, performant Metal/C++ hot paths, consistent diagnostics/contracts, memory-aware execution, real-fixture proof, and no half-stubbed behavior hidden behind successful tests.

## Shape Context
Parity closure target: move the opt-in native chart candidate closer to the intended Pixal3D export path while keeping xatlas chart parity explicitly deferred until proven.

Coverage risk areas: Phase 21 reports `uv_surface_occupancy_ratio=0.5232105255126953`, `uv_surface_final_visible_coverage_ratio=0.7556368090466`, and `global_coverage_ratio=0.3953571319580078`. Since occupancy already exceeds `0.50`, filling remaining UV-surface holes can theoretically clear global coverage.

## Constraints
- Keep implementation native, bounded, and memory-aware; Python must not own the texture hot path.
- Preserve exact/raw sampling diagnostics while adding final-fill diagnostics.
- Keep generated and heavy artifacts under `/tmp`.
- Respect Automaton lifecycle and commit only after verified gates pass.

## Scope Coverage
- Included: native UV-surface hole fill, texture diagnostics, candidate-readiness assertions, real Pixal3D heavy fixture proof, docs, package/root/build verification.
- Deferred: xatlas chart parity, further chart splitting, decoded model output changes, default backend switch, release/tag/push/publish.
- Anti-goals: relaxing quality thresholds, hiding raw coverage, writing heavy artifacts into the repo.

## Scope Preservation
This intentionally decomposes the broader native-export-backend goal into the next bounded coverage phase. The broader goal remains active after this phase.

## Recommended Approach
Surface fill: add an O(texture pixels) native UV-surface fill for remaining missing surface texels because current occupancy is above the floor and global coverage is now gated by surface visibility.

## Key Assumptions and Risks
- Assumption: the missing UV-surface region is connected enough to existing visible texels for bounded surface fill to raise final global coverage above `0.50`.
- Risk: fill can smear colors if overused. Mitigation: keep raw/exact coverage diagnostics and report the number of surface-filled texels separately.

## Rejected or Deferred
- More chart splitting was deferred because global coverage can now cross the floor by improving surface visibility.
- Diagnostics-only was deferred because current diagnostics already identify surface visibility as the next bounded blocker.

## Next Step
Frame the UV surface fill gate as a SPEC with real-fixture coverage readiness and honest diagnostics.
