# Intake: Polish Generation Pipeline Scripts

Generated by auto-office-hours on 2026-05-24
Active change: 2026-05-24-polish-generation-pipeline-scripts
Status: DRAFT
Mode: Builder
Work scale: feature
Work shape: refactor

## Objective
Make the existing scripts under the current repo read as self-documenting `mlx-spatial` generation pipeline scripts, with proper names, clear recommended settings, and clearer examples of the intended MLX workflow.

## Broader Intent
The scripts should be easy for humans and agents to inspect as the repo-local generation pipeline surface, without needing to reverse-engineer which settings are recommended for each model family.

## Mode Context
What makes this useful is that the scripts become readable canonical examples for running SAM3D, TRELLIS.2, HY-WorldMirror, and LiTo from this repo.

## Shape Context
Structural goal: keep the existing script layout and wrapper behavior, but make the current script surface more consistent through naming clarity, docstrings, help text, and README alignment.

## Constraints
- Keep the current model-family grouping under `scripts/`.
- Keep this as a polish pass over the existing scripts, not a pipeline rewrite.
- Preserve current recommended runtime behavior unless live inspection shows a script and its docs disagree.
- Leave unrelated Automaton/skill worktree changes untouched.

## Scope Coverage
- Included: existing generation scripts under `scripts/sam3d/`, `scripts/trellis2/`, `scripts/hyworld2/`, and `scripts/lito/`.
- Included: script names or titles where they are unclear, script docstrings, command help text, printed effective settings, and `scripts/README.md` alignment.
- Included: making recommended settings visible and consistent for each model family.
- Deferred: converting the scripts into direct Python pipeline API examples; that was Approach B and is outside this selected pass.
- Deferred: a larger examples-vs-maintainer-tools split; that was Approach C and is outside this selected pass.
- Anti-goals: no model behavior rewrite, no directory reshuffle, no release/tag/publish work, no generated outputs, and no changes to unrelated dirty files.

## Scope Preservation
This intentionally narrows the clarified intent to Approach A: polish the existing script surface now, while preserving the larger "canonical pipeline examples" direction as later work if needed.

## Recommended Approach
Approach A: polish existing scripts because the current need is clarity and consistency, not a larger framework-example rewrite.

## Key Assumptions and Risks
- Assumption: current recommended settings are mostly correct and need clearer surfacing rather than recalibration.
- Risk: changing file paths for "proper names" could create churn in docs and user commands, so path renames should happen only when a current name is clearly misleading.

## Rejected or Deferred
- Approach B, canonical pipeline examples, is deferred because it would rewrite several scripts around direct Python API usage.
- Approach C, formal example suite plus maintainer split, is deferred because it is larger than the approved polish pass.

## Next Step
Frame the script-polish pass into a compact SPEC with acceptance criteria for script self-documentation, naming clarity, recommended settings, and verification.
