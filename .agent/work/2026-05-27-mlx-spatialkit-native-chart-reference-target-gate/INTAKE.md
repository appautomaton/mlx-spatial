# Intake: mlx-spatialkit Native Chart Reference-Target Gate

Generated by auto-office-hours on 2026-05-27
Active change: 2026-05-27-mlx-spatialkit-native-chart-reference-target-gate
Status: DRAFT
Mode: Builder
Work scale: feature
Work shape: mixed

## Objective
Codify the passing `reference-target` native-chart path with real-fixture tests and docs before moving to heavier upstream settings.

## Broader Intent
Turn `mlx-spatialkit` into the dependable native export backend for `mlx-spatial`: quality-first geometry, performant Metal/C++ hot paths, consistent diagnostics/contracts, memory-aware execution, real-fixture proof, and no half-stubbed behavior hidden behind successful tests.

## Shape Context
Parity closure target: prove that the opt-in native chart backend works under the checked-in Pixal3D reference-target export settings while keeping xatlas chart parity and 1M/4096 upstream-setting parity explicitly deferred.

Coverage risk areas: a `/tmp` probe for `quality_preset="reference-target", uv_backend="native-chart"` reports `production_quality_ready=true`, `native_chart_uv_candidate.status=quality_ready`, `global_coverage_ratio=0.5047416687011719`, visual comparison `all_passed=true`, and xatlas/1M parity still deferred.

## Constraints
- Keep generated and heavy artifacts under `/tmp`.
- Do not change runtime behavior unless the readiness test exposes a real gap.
- Respect Automaton lifecycle and commit only after verified gates pass.

## Scope Coverage
- Included: heavy reference-target native-chart gate, diagnostics assertions, docs, package/root/build verification.
- Deferred: 1M/4096 native-chart hardening, browser-render native-chart proof, xatlas chart parity.
- Anti-goals: relaxing thresholds, changing decoded model outputs, switching the default UV backend, tagging/pushing/publishing/releasing.

## Scope Preservation
This intentionally decomposes the broader native-export-backend goal into a readiness-evidence phase. The broader goal remains active after this phase.

## Recommended Approach
Reference native-chart: codify the passing probe because the next reliable step is to make the reference-target native-chart path a durable regression gate before heavier upstream-setting work.

## Key Assumptions and Risks
- Assumption: the probe is stable enough to promote into a heavy test.
- Risk: the gate adds a heavier test path. Mitigation: keep it `@pytest.mark.heavy` and run it explicitly in this phase.

## Rejected or Deferred
- 1M native-chart was deferred because reference-target native-chart readiness should be locked down first.
- Browser proof was deferred because scalar and deterministic GLB/PNG visual comparison already passed in the probe.

## Next Step
Frame and plan the reference-target native-chart gate, then add focused heavy assertions and docs.
