# Intake: mlx-spatialkit Low-Fill Chart Split Gate

Generated by auto-office-hours on 2026-05-27
Active change: 2026-05-27-mlx-spatialkit-low-fill-chart-split-gate
Status: DRAFT
Mode: Builder
Work scale: feature
Work shape: mixed

## Objective
Add deterministic low-fill chart splitting to improve native chart rect fill and global texture coverage without changing bake thresholds.

## Broader Intent
Turn `mlx-spatialkit` into the dependable native export backend for `mlx-spatial`: quality-first geometry, performant Metal/C++ hot paths, consistent diagnostics/contracts, memory-aware execution, real-fixture proof, and no half-stubbed behavior hidden behind successful tests.

## Shape Context
Parity closure target: move the opt-in native chart candidate closer to the intended Pixal3D export path while keeping xatlas chart parity explicitly deferred until proven.

Coverage risk areas: low chart-internal rectangle fill is the current measured native-chart quality blocker. The latest real fixture reports `chart_rect_fill_ratio=0.5727071617508422`, `uv_surface_occupancy_ratio=0.5145282745361328`, `uv_surface_final_visible_coverage_ratio=0.7548626376681581`, `global_coverage_ratio=0.3883981704711914`, and `quality_blockers=["global_coverage_floor"]`.

## Constraints
- Keep implementation native C++ and deterministic for identical inputs.
- Keep runtime and memory bounded; splitting must not create unbounded chart counts or UV-bin references.
- Keep generated and heavy artifacts under `/tmp`.
- Respect Automaton lifecycle and commit only after verified gates pass.

## Scope Coverage
- Included: low-fill chart split logic, split diagnostics, focused geometry tests, real Pixal3D heavy fixture proof, docs, package/root/build verification.
- Deferred: xatlas parity, LSCM/ABF unwrap, overlap solving, default UV backend switch, decoded model output changes, Metal bake-kernel changes.
- Anti-goals: relaxing quality thresholds, masking `global_coverage_floor`, writing heavy artifacts into the repo, tagging/pushing/publishing/releasing.

## Scope Preservation
This intentionally decomposes the broader native-export-backend goal into the next bounded quality phase. The broader goal remains active after this phase.

## Recommended Approach
Chart split: split low-fill charts deterministically by projected footprint/face distribution because source evidence shows UV occupancy is barely above its floor and post-fill probes plateaued below the global coverage floor.

## Key Assumptions and Risks
- Assumption: splitting low-fill charts can raise `chart_rect_fill_ratio` and/or `global_coverage_ratio` more directly than additional dilation.
- Risk: over-splitting may increase duplicated vertices, chart count, and UV-bin candidate references. Mitigation: bounded split criteria, diagnostics, focused tests, and real fixture guard assertions.

## Rejected or Deferred
- Bake fill was deferred because Phase 20 evidence says additional dilation probes plateau around `0.393` global coverage.
- Diagnostics-only was deferred because the current diagnostics are strong enough to select a bounded next quality lever.

## Next Step
Frame the low-fill chart split gate as a SPEC with measurable real-fixture improvement and truthful remaining blockers.
